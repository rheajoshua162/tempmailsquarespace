# Email Mode: 'imap' (fetch from Gmail) or 'smtp' (receive directly on VPS)
EMAIL_MODE=smtp

# ============ SMTP MODE (Recommended) ============
# VPS receives emails directly - no Gmail needed
SMTP_PORT=25
SMTP_HOST=0.0.0.0

# ============ IMAP MODE (Alternative) ============
# Fetch emails from Gmail via IMAP
# Only needed if EMAIL_MODE=imap
GMAIL_USER=your-email@gmail.com
GMAIL_PASS=your-app-password
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
POLLING_INTERVAL_MS=2000

# ============ Server Configuration ============
PORT=3001
FRONTEND_URL=http://localhost:5173
NODE_ENV=production

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Session Settings
INBOX_EXPIRY_MINUTES=20

# Hold Feature - Password to permanently hold usernames
HOLD_PASSWORD=sempak007